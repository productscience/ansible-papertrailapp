description "Monitor files and send to remote syslog"
start on runlevel [2345]
stop on runlevel [!2345]

respawn

pre-start exec /usr/bin/test -e {{  papertrail_conf_file }}

exec {{ papertrail_remote_syslog }} -D -c "{{ papertrail_conf_file }}"
